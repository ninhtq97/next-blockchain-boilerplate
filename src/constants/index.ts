export enum Games {
  BNZ = 'bnz',
  IZZY = 'izzy',
}

export const PAGE = {
  DEFAULT: 1,
  SIZE: 10,
};

export const BATCH_SIZE = 100;

export const REFRESH_BLOCK_INTERVAL = 3000;

export const REGEXP = {
  DIGIT: /^\d+$/,
};
